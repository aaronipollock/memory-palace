import React from 'react';
import { Canvas } from 'react-three-fiber';

const MemoryPalace = ({ images }) => {
    return (
        <Canvas>
            {/* Render 3D room and images here */}
        </Canvas>
    );
};

export default MemoryPalace;
